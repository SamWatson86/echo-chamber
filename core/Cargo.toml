[workspace]
members = [
  "control",
  "client",
  "admin-client",
  "hook",
]
default-members = [
  "control",
  "client"
]
resolver = "2"

[patch.crates-io]
livekit = { path = "livekit-local" }
libwebrtc = { path = "libwebrtc-local" }
webrtc-sys = { path = "webrtc-sys-local" }
